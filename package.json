{
  "name": "pi-agent",
  "version": "0.0.15",
  "description": "Chat with Pi using context from your notes, links, backlinks, tags, and search results.",
  "private": true,
  "scripts": {
    "build": "node scripts/build.mjs",
    "build:check": "node scripts/build.mjs --check",
    "format": "prettier --write --no-error-on-unmatched-pattern AGENTS.md main.js styles.css src/**/*.js src/**/*.mjs tests/**/*.mjs scripts/*.mjs eslint*.config.mjs docs/*.md *.md *.json .github/workflows/*.yml",
    "format:check": "prettier --check --no-error-on-unmatched-pattern AGENTS.md main.js styles.css src/**/*.js src/**/*.mjs tests/**/*.mjs scripts/*.mjs eslint*.config.mjs docs/*.md *.md *.json .github/workflows/*.yml",
    "lint": "eslint src scripts tests",
    "lint:obsidian": "eslint --config eslint.obsidian.config.mjs src package.json",
    "lint:obsidian:errors": "npm run lint:obsidian -- --quiet",
    "test": "vitest run",
    "test:pi": "node scripts/pi-smoke.mjs",
    "typecheck": "tsc --noEmit",
    "ci": "npm run build && npm run build:check && npm run format:check && npm run lint && npm run lint:obsidian:errors && npm run typecheck && npm test && npm run version:check",
    "dev:install": "node scripts/install-dev.mjs",
    "version:check": "node scripts/check-version.mjs",
    "release:zip": "node scripts/package-release.mjs",
    "release:notes": "node scripts/extract-release-notes.mjs"
  },
  "keywords": [
    "obsidian",
    "obsidian-plugin",
    "pi",
    "ai",
    "agent"
  ],
  "author": "Christian Lempa",
  "license": "MIT",
  "devDependencies": {
    "@codemirror/state": "^6.5.0",
    "@codemirror/view": "^6.38.6",
    "@eslint/js": "^10.0.1",
    "@types/node": "^25.6.0",
    "esbuild": "^0.28.1",
    "eslint": "^10.3.0",
    "eslint-plugin-obsidianmd": "^0.4.1",
    "obsidian": "^1.12.3",
    "prettier": "^3.8.3",
    "typescript": "^6.0.3",
    "vitest": "^4.1.5"
  }
}
