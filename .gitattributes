# Keep line endings consistent across platforms so Prettier's format check
# behaves the same on Windows and Linux.
* text=auto eol=lf

*.png binary
*.jpg binary
*.jpeg binary
*.pdf binary
*.zip binary
*.pxd binary
